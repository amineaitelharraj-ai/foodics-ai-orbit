REPLIT FRONTEND UPDATE: Large Data Notice (Merge Into Spinner)
================================================================

GOAL: When backend sends `large_data_notice`, show it INSIDE the spinner - not as a separate card.

VISUAL:
┌─────────────────────────────────────────────────────────────────┐
│ 🥙 Preparing your answer                                        │
│    "Slow-cooking your Kabsa to perfection..."                   │
│                                                                 │
│    ────────────────────────────────────────                     │
│    📊 Fetching large dataset...  Explore BI Tools →             │
└─────────────────────────────────────────────────────────────────┘

================================================================
STEP 1: ADD STATE
================================================================

// In your chat component state
const [isLargeDataFetch, setIsLargeDataFetch] = useState(false);

================================================================
STEP 2: UPDATE WEBSOCKET HANDLER
================================================================

case 'large_data_notice':
  setIsLargeDataFetch(true);
  break;

case 'tool_end':
  setIsLargeDataFetch(false);
  // ... existing handling
  break;

case 'complete':
  setIsLargeDataFetch(false);
  // ... existing handling
  break;

================================================================
STEP 3: UPDATE SPINNER COMPONENT
================================================================

Pass `isLargeDataFetch` as prop to your spinner, then add this after the fun message:

{isLargeDataFetch && (
  <div className="large-data-indicator">
    <span>📊</span>
    <span>Fetching large dataset...</span>
    <a href="/ai-orbit/insights">Explore BI Tools →</a>
  </div>
)}

================================================================
STEP 4: CSS
================================================================

.large-data-indicator {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-top: 12px;
  padding-top: 12px;
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  font-size: 13px;
  color: rgba(255, 255, 255, 0.7);
}

.large-data-indicator a {
  color: #64B5F6;
  text-decoration: none;
  margin-left: auto;
}

.large-data-indicator a:hover {
  color: #90CAF9;
}

================================================================
STEP 5: REMOVE OLD IMPLEMENTATION
================================================================

Delete any separate <LargeDataNotice> component or chat bubble for this event.

================================================================
TEST
================================================================

Send: "list all my products"

Expected: Spinner shows fun message + "📊 Fetching large dataset... Explore BI Tools →" below it.
